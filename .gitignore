# If .gitignore doesn't exist, create it with standard Go entries
# If it exists, ensure Swagger docs aren't ignored

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
unisphere-api

docker-compose.yml

# Test binary, built with `go test -c`
*.test

configs/config.yaml
uploads/


# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Go workspace file
go.work

# IDE files
.idea/
.vscode/
*.swp
*.swo

# Dependency directories (remove if vendoring is used)
/vendor/

# Environment files
.env
.env.local

# Logs
*.log

# Build and temporary files
/tmp/
/dist/

# Keep Swagger docs
!/docs/
!/docs/swagger.json
!/docs/swagger.yaml
!/docs/docs.go

# OS files
.DS_Store
Thumbs.db

**/.claude/settings.local.json
